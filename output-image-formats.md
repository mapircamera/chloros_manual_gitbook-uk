---
metaLinks:
  alternates:
    - https://app.gitbook.com/s/o044KN3Ws0uIDvOmSkcR/output-image-formats
---

# Формати вихідних зображень

### TIFF (16-бітний)

16-бітний (цифрове значення пікселя від 0 до 65535) Вихід у форматі TIFF (.tif).

### TIFF (32-бітний, відсотки %)

32-бітний (плаваюче значення пікселя 0,0–1,0) вихід у форматі TIFF (.tif). Значення пікселів представляють відсотки відбиття, де 0,0 = 0% і 1,0 = 100%.

### **JPG (8-біт)**

8-бітний (цифрове значення пікселя 0–255) вихід у форматі JPG (.jpg).

### **PNG (8-бітний)**

8-бітний (цифрове значення пікселя 0–255) формат виводу PNG (.png). Стиснення без втрат, придатне для перегляду в Інтернеті та візуалізації.

